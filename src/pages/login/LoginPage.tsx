import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <main className="pt-16 flex justify-center items-center min-h-[calc(100vh-64px)]">
      <LoginForm />
    </main>
  );
};

export default LoginPage;
